import React from 'react'
import PropTypes from 'prop-types'
import Layout from './Layout'

const Cart = (props) => {
  return (
    <Layout>
      <div>
        <p>Not items in the cart</p>
      </div>
    </Layout>
  )
}

Cart.propTypes = {}

export default Cart
